// src/lib/animations/anims.js


export function writeFn(el, state) {
  // placeholder: transition logic for GSAP animations on el
  // LEGACY: anims.js → writeFn
}

export function writeCt(el, state) {
  // placeholder: alternative animation function
  // LEGACY: anims.js → writeCt
}